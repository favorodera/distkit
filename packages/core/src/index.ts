import type { UserConfig } from '@distkit/cli/types'

/**
 * Defines the project's Distkit configuration.
 * @param configInput The configuration for the project.
 * @returns The provided configuration object.
 */
export function defineConfig(configInput: UserConfig) {
  return configInput
}
